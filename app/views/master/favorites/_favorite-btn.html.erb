<% if movie.favorited_by?(current_user)  %>
              <p>
                <%= link_to movie_favorites_path(book), method: :delete, remote:true  do %>
                  ♥<%= movie.favorites.count %>
                <% end %>
              </p>
              <% else %>
              <p>
                <%= link_to movie_favorites_path(movie), method: :post, remote:true do %>
                  ♡<%= movie.favorites.count %>
                <% end %>
              </p>
                <% end %>